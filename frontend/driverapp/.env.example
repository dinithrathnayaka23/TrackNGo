# Optional. Leave unset to let Expo use the current dev host on port 8080.
# Use this when your backend is not on the same host as the Expo dev server.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
